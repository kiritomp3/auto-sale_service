services:
  wb-cards-api:
    build: .
    container_name: wb-cards-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:18000:8000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./tmp:/app/tmp
